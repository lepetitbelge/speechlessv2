<h1>Landing Page</h1>
<ul>
  <% @speeches.each do |speech| %>
    <li>
      <%= link_to speech.title, speech_path(speech) %>
    </li>
  <% end %>
</ul>
<!-- Later that will be the search: -->
<%= link_to 'Search', speeches_path %>