<div class="text-center">
  <h2>Speeches Index</h2>
</div>

<div class="text-center">
  <ul>
    <% @speeches.each do |speech| %>
      <li><%= link_to speech.title, speech_path(speech) %></li>
      <br>
    <% end %>
  </ul>
</div>
